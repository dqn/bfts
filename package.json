{
  "name": "bfts",
  "version": "0.1.1",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "test": "tsc ./test/index.ts --noEmit",
    "unlimit": "sed -i -e 's/instantiationDepth === 50 || instantiationCount >= 5000000/false/' node_modules/typescript/lib/{tsc,tsserver}.js",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "prettier": "2.2.1",
    "typescript": "4.1.2"
  }
}
